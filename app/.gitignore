node_modules/
dist/
.env
.env.local
.env.*.local

# Rendered audio is an output artifact. Uncomment the exclusion below to keep
# rendered audio out of the repo during development; remove the comment (so
# rendered audio IS committed) when preparing the canonical archival bundle.
# assets/audio/rendered/
